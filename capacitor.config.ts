import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'coleccion-de-animales-domesticos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
